name: $(Major).$(Minor)$(Rev:.r)
pool:
  vmImage: 'windows-latest'
variables:
- group: Versioning
- name: BuildPlatform
  value: 'Any CPU'
- name: BuildConfiguration
  value: 'Release'

steps:
- template: 'Template_Build.yml'
  parameters:
    solutions: '**\\*.sln'